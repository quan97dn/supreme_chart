// Cores
import 'package:flutter/material.dart';

class LineChart {
  String text;
  bool isActive;
  int value;
  Offset offset;
  LineChart({
    this.text,
    this.isActive = false,
    this.value,
    this.offset,
  });
}
